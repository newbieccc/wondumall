<?xml version="1.0"	encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"	
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="payment">
	<insert id="checkout" parameterType="orderDTO">
	INSERT INTO order (o_name, o_email, o_postcode, o_roadAddress, o_extraAddress, o_detailAddress, ) VALUES (#{o_name}, #{o_email}, #{o_postcode}, #{o_roadAddress}, #{o_extraAddress}, #{o_detailAddress})
	</insert>
	<select id="product" resultType="productDTO">
	SELECT * FROM product WHERE p_no=#{p_no}
	</select>
	<select id="user" resultType="userDTO">
	SELECT * FROM user WHERE u_no=#{u_no}
	</select>
</mapper>