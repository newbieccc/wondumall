<?xml version="1.0"	encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"	
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="payment">
	<insert id="checkout" parameterType="orderDTO">
		INSERT INTO orderorder(o_name, o_email, o_postcode, o_roadAddress, o_extraAddress, o_detailAddress, o_tel, o_request, imp_uid, merchant_uid, u_no, o_pname, o_price)
		VALUES (#{o_name}, #{o_email}, #{o_postcode}, #{o_roadAddress}, #{o_extraAddress}, #{o_detailAddress}, #{o_tel}, #{o_request}, #{imp_uid}, #{merchant_uid}, #{u_no}, #{o_pname}, #{o_price})
	</insert>
	<select id="product" parameterType="productDTO" resultType="productDTO">
		SELECT * FROM product WHERE p_no=#{p_no}
	</select>
	<select id="user" resultType="userDTO">
		SELECT * FROM user WHERE u_no=#{u_no}
	</select>
	<select id="couponList" resultType="couponDTO">
		SELECT * FROM coupon WHERE coupon_del=0
	</select>
	<select id="cartPay" parameterType="integer" resultType="cartDTO">
		SELECT *
		FROM cartview
		WHERE p_check = 1 AND u_no = #{u_no}
	</select>
	<select id="orderList" parameterType="orderDTO" resultType="orderDTO">
		SELECT *
		FROM orderorder
		WHERE u_no=#{u_no}
	</select>
	
	<update id="setStatus" parameterType="map">
		UPDATE orderorder SET o_status = "환불완료"
		WHERE u_no = #{u_no} AND merchant_uid = #{merchant_uid}
	</update>
</mapper>